server:
  port: 8100
spring:
  application:
    name: nfsy-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/properties/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/